spring:
  config:
    activate:
      on-profiles: local

  data:
    redis:
      host: localhost
      port: 6379

app:
  services:
    auth-uri: http://localhost:8082
    ticket-uri: http://localhost:8084
    notification-uri: http://localhost:8083