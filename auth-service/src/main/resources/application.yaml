server:
  port: 8082

spring:
  application:
    name: auth-service

  datasource:
    url: jdbc:postgresql://localhost:${AUTH_DB_PORT}/auth_db
    username: ${AUTH_DB_USER}
    password: ${AUTH_DB_PASSWORD}
